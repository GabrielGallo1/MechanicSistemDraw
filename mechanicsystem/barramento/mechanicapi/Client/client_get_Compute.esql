BROKER SCHEMA Client

CREATE COMPUTE MODULE client_get_Compute
  CREATE FUNCTION Main() RETURNS BOOLEAN
  BEGIN
    DECLARE id CHARACTER InputLocalEnvironment.REST.Input.Parameters.id;

    -- URL correta para o backend
    SET OutputLocalEnvironment.Destination.HTTP.RequestURL =
      'http://localhost:3333/clients/' || id;

    -- Método HTTP correto
    SET OutputLocalEnvironment.Destination.HTTP.RequestLine.Method = 'GET';

    RETURN TRUE;
  END;
END MODULE;